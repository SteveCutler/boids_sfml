TO DO

correct collision for boid size, global variable
add collision objects
    create collision point list? that way I can say if new pos in collision area reverse, which i thin k is O(1)

optimize neighbour search by creating cell structure

write visual debugger to display velocity vectors of the boids

replace pow with x * x for performance

Add performance metric overlays
Optimize performance in iterations

** write safer font code
** hardcoded font file